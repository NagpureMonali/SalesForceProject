public with sharing class ParameterisedConstructor // to shoe demo of parameterized constructor
{
	 public ParameterisedConstructor(String s) // consttructor
     {
      //  System.debug('hello... ' +s);
        System.debug(s + ' this is parameterized constructor.');
     }
}